server.port: 8092

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/refdata
    username: refdata
    password: refdata
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration